apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-events-resources
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: production
  source:
    path: argo-events-resources
    repoURL: https://github.com/randommm/argo-combined-demo
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
